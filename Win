class WinScreen{ //creates the ending screen
  
  void drawWin(){ //draws the win screen
    textAlign(CENTER);
    textSize(65);
    fill(150,255,255);
    text("CONGRATION",width/2,height/4);
    textSize(50);
    text("YOU DONE IT!!!",width/2,350);
  }
}
